{
  "type": "object",
  "title": "ISphere",
  "description": "Part::Sphere",
  "required": ["radius"],
  "additionalProperties": false,
  "properties": {
    "radius": {
      "type": "number",
      "description": "Radius of the sphere"
    },
    "center": {
      "type": "array",
      "description": "Coordinate of the center",
      "items": {
        "type": "number"
      }
    }
  }
}
